# 用一个例子走完 Proposition 1 的证明：$m=3,\ s=2$

这篇讲解专门解释 [完整证明](proof-walkthrough.md) 中最容易卡住的地方：**为什么可以把 $G_1+G_2$ 换成 $G_2$，又为什么可以把 $U+G_1$ 换成 $U$？** 我们会使用原证明的真实参数，而不只是画一个不满足误差要求的示意图。

本文把最终允许的全变差距离定为 $\varepsilon=1/2$。目标是随机生成三个严格递增的日期 $D_1<D_2<D_3$，使以下三种双日期观察结果的分布两两相距不超过 $1/2$：

$$
D_{\{1,2\}}=(D_1,D_2),\qquad
D_{\{1,3\}}=(D_1,D_3),\qquad
D_{\{2,3\}}=(D_2,D_3).
$$

先记住贯穿全文的一句话：

> **证明中的“替换”是替换概率分布，不是说同一次抽样中的两个数相等，也不是说它们每次都相差很小。**

## 1. 先借用已经会做的“一次只看一个日期”

我们正在证明 $s=2$，所以归纳假设要用 $s-1=1$。新日历将有 $m=3$ 个日期、两个相邻间隔，因此旧日历需要 $m-1=2$ 个日期。

先把最终误差的三分之一留给旧日历：

$$
\frac{\varepsilon}{3}=\frac16.
$$

令 $V$ 在 $\{1,2,3,4,5,6\}$ 中等概率取值，并设

$$
X_1=V,\qquad X_2=V+1.
$$

于是 $X_1$ 均匀取 $1,\ldots,6$，$X_2$ 均匀取 $2,\ldots,7$。两者的概率表只在结果 $1$ 和 $7$ 处不同：

| 可能结果 | 1 | 2 | 3 | 4 | 5 | 6 | 7 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| $X_1$ 的概率 | $1/6$ | $1/6$ | $1/6$ | $1/6$ | $1/6$ | $1/6$ | $0$ |
| $X_2$ 的概率 | $0$ | $1/6$ | $1/6$ | $1/6$ | $1/6$ | $1/6$ | $1/6$ |

因此

$$
d_{\mathrm{TV}}(X_1,X_2)
=\frac12\left(\frac16+\frac16\right)
=\frac16.
$$

**容易混淆的一点：**在同一次旧日历抽样中，$X_2=X_1+1$，它们从来不相等。上式比较的是它们各自的**分布**。旧日期的最大值是 $7$，所以原证明中的 $\ell=7$。

## 2. 用旧日期决定两个新间隔

按原证明的参数公式，取

$$
q=1+\left\lceil\frac{6(s-1)}{\varepsilon}\right\rceil
=1+\left\lceil\frac{6}{1/2}\right\rceil
=13,
$$

以及

$$
S=(m-1)q^\ell=2\cdot13^7,
\qquad
H=\left\lceil\frac{6S}{\varepsilon}\right\rceil=12S.
$$

$S$ 是两个新间隔**总和的最坏情况上界**，$H$ 是新随机起点的取值范围。这些数字看起来很大，是因为证明只要求“有限”，并采用方便统一估计的保守参数；理解逻辑时无需计算 $13^7$ 的十进制展开。

给定旧日历 $X=(X_1,X_2)$ 后，独立抽取

$$
G_1\mid X\sim\operatorname{Unif}[13^{X_1}],
\qquad
G_2\mid X\sim\operatorname{Unif}[13^{X_2}].
$$

这里 $\operatorname{Unif}[N]$ 指在整数 $1,\ldots,N$ 上均匀分布。再独立抽取 $U\sim\operatorname{Unif}[H]$，构造

$$
D_1=U,\qquad
D_2=U+G_1,\qquad
D_3=U+G_1+G_2.
$$

两个 $G$ 都是正整数，所以每次生成都满足 $D_1<D_2<D_3$。又因为 $X_1,X_2\leq7$，所以 $G_1+G_2\leq2\cdot13^7=S$，从而 $D_3\leq H+S$。这说明新日历确实有**有限**的上界。

一个具体的抽样结果可以帮助理解记号：若 $V=2$，就有 $X=(2,3)$；此时 $G_1$ 从 $1,\ldots,169$ 中抽，$G_2$ 从 $1,\ldots,2197$ 中抽。如果这一轮恰好抽到 $U=100,G_1=5,G_2=40$，新日历就是

$$
D=(100,105,145).
$$

接下来的全变差距离**不能**从这一轮的三个数算出；必须看反复生成日历时，各种结果出现的概率。

## 3. 为什么先把“两个日期”改写为“首日 + 间隔”？

对任意两个按时间排列的日期 $(a,b)$，知道 $(a,b)$ 与知道 $(a,b-a)$ 完全等价：从 $(a,b-a)$ 可以把第二个日期加回来。因此这个改写只是给结果重新命名，不改变两个分布之间的全变差距离。

三种观察方式改写后分别是：

| 选择的位置 | 实际观察的日期 | 改写为“首日 + 间隔”后的记录 |
| --- | --- | --- |
| $\{1,2\}$ | $(U,U+G_1)$ | $Y_{\{1,2\}}=(U,G_1)$ |
| $\{1,3\}$ | $(U,U+G_1+G_2)$ | $Y_{\{1,3\}}=(U,G_1+G_2)$ |
| $\{2,3\}$ | $(U+G_1,U+G_1+G_2)$ | $Y_{\{2,3\}}=(U+G_1,G_2)$ |

现在要处理的“麻烦”一眼就看出来了：第二行有 $G_1+G_2$，第三行有 $U+G_1$。我们希望把三行变成更容易比较的理想记录：

| 选择的位置 | 实际记录 $Y_I$ | 理想记录 $\widetilde Y_I$ | 要处理的地方 |
| --- | --- | --- | --- |
| $\{1,2\}$ | $(U,G_1)$ | $(U,G_1)$ | 无须处理 |
| $\{1,3\}$ | $(U,G_1+G_2)$ | $(U,G_2)$ | 去掉加在 $G_2$ 上的 $G_1$ |
| $\{2,3\}$ | $(U+G_1,G_2)$ | $(U,G_2)$ | 去掉加在 $U$ 上的 $G_1$ |

例如在上一节的**那一轮抽样**里，三种实际记录是 $(100,5)$、$(100,45)$、$(105,40)$；理想形式对应 $(100,5)$、$(100,40)$、$(100,40)$。这些单次数字只帮助我们核对“哪一项被替换”，不构成概率分布接近的证明。

## 4. 理解两次替换，只需先懂一个均匀平移例子

让 $Z$ 在整数 $1,\ldots,100$ 上均匀分布。$Z+5$ 则在 $6,\ldots,105$ 上均匀分布。两者虽然每次都相差 $5$，却在 $6,\ldots,100$ 这 **95 个结果**上有相同的概率，因而

$$
d_{\mathrm{TV}}(Z+5,Z)=\frac5{100}=0.05.
$$

一般地，若 $Z\sim\operatorname{Unif}[N]$，平移一个固定的非负整数 $a$，则

$$
d_{\mathrm{TV}}(Z+a,Z)
=\min\left\{\frac aN,1\right\}.
$$

若平移量 $A$ 本身随机，并且**与 $Z$ 独立**，就先固定 $A=a$ 使用上式，再对 $A$ 的取值求平均：

$$
d_{\mathrm{TV}}(Z+A,Z)
\leq\mathbb E\min\left\{\frac AN,1\right\}
\leq\frac{\mathbb EA}{N}.
\tag{平移估计}
$$

下面的两次替换，其实只是把这个公式分别用于 $Z=G_2$ 和 $Z=U$。

### 4.1 为什么 $(U,G_1+G_2)$ 接近 $(U,G_2)$？

**先固定旧日历的值 $V=v$。** 此时 $X_1=v,X_2=v+1$，所以

$$
G_1\sim\operatorname{Unif}[13^v],
\qquad
G_2\sim\operatorname{Unif}[13^{v+1}],
$$

并且 $G_1$ 与 $G_2$ 独立。把上面的平移估计应用于

$$
Z=G_2,\quad A=G_1,\quad N=13^{v+1},
$$

得到

$$
\begin{aligned}
d_{\mathrm{TV}}(G_1+G_2,G_2\mid V=v)
&\leq\frac{\mathbb E(G_1\mid V=v)}{13^{v+1}}\\
&=\frac{13^v+1}{2\cdot13^{v+1}}\\
&\leq\frac1{13}
<\frac1{12}.
\end{aligned}
$$

**通俗理解：**$G_2$ 是从一个很宽的均匀范围中抽出来的；给它加上 $G_1$，只改变边缘上的一小部分概率。这里说的是**分布**接近，不是保证每次抽出的 $G_2$ 一定大于 $G_1$。

$U$ 与这些间隔独立。给两边同时附上相同的第一个坐标 $U$，全变差距离不变：

$$
d_{\mathrm{TV}}\bigl((U,G_1+G_2),(U,G_2)\mid V=v\bigr)
=d_{\mathrm{TV}}(G_1+G_2,G_2\mid V=v)
\leq\frac1{12}.
$$

这个界对所有 $v$ 都一样，再对 $V$ 求平均，整个二元记录的无条件距离仍至多 $1/12$。

### 4.2 为什么 $(U+G_1,G_2)$ 接近 $(U,G_2)$？

这次把平移估计应用于

$$
Z=U,\quad A=G_1,\quad N=H.
$$

$U$ 独立于 $G_1$，而 $G_1\leq S$，故

$$
d_{\mathrm{TV}}(U+G_1,U\mid V=v)
\leq\frac{\mathbb E(G_1\mid V=v)}H
\leq\frac SH
=\frac1{12}.
$$

**通俗理解：**$U$ 在一个极宽的范围内均匀变化，额外加上一个至多为 $S$ 的间隔，只会移动这个大范围的一小部分边缘。

但是这里比较的是**两个坐标组成的记录**，不能只看第一个坐标就停下。固定 $V=v$ 后，$G_2$ 与 $(U,G_1)$ 独立。因此两边附上相同的第二个坐标 $G_2$，全变差距离不变：

$$
d_{\mathrm{TV}}\bigl((U+G_1,G_2),(U,G_2)\mid V=v\bigr)
=d_{\mathrm{TV}}(U+G_1,U\mid V=v)
\leq\frac1{12}.
$$

再对 $V$ 求平均，就得到两个二元记录的无条件距离也至多 $1/12$。

**为什么反复强调“固定 $V=v$”？** 因为 $G_1$ 与 $G_2$ 的取值范围都由同一个随机的 $V$ 决定。它们给定 $V$ 后独立，无条件下通常相关；直接宣称无条件独立会把证明讲错。

## 5. 三种记录已变简单：旧日历在哪里发挥作用？

理想记录只有两种：

$$
(U,G_1)\qquad\text{和}\qquad(U,G_2).
$$

它们分别来自旧日期 $X_1$ 和 $X_2$，而处理规则完全相同：输入一个旧日期 $x$，就独立抽取 $U\sim\operatorname{Unif}[H]$ 和 $G\sim\operatorname{Unif}[13^x]$，输出 $(U,G)$。

**对两个分布做同样的随机处理，不会让它们更容易被区分。** 因为旧日期已经满足 $d_{\mathrm{TV}}(X_1,X_2)=1/6$，所以

$$
d_{\mathrm{TV}}\bigl((U,G_1),(U,G_2)\bigr)
\leq d_{\mathrm{TV}}(X_1,X_2)
=\frac16.
$$

这里的 $U,G_1,G_2$ 指各自的**概率分布**。虽然实际生成的同一份日历里总有 $X_2=X_1+1$，不能把旧日历的这两个数说成“相等”，它们的分布却只差 $1/6$；共同的后续随机处理保留了这个分布距离上界。

## 6. 最后把误差相加

“日期对 ↔ 首日与间隔”是可逆改写，所以比较日期对的分布距离，等于比较相应 $Y_I$ 的分布距离。下面把每对记录沿着“实际 → 理想 → 理想 → 实际”的路线比较：

| 比较哪两种位置 | 第一份记录的替换误差 | 两份理想记录的距离 | 第二份记录的替换误差 | 总上界 |
| --- | ---: | ---: | ---: | ---: |
| $\{1,2\}$ 与 $\{1,3\}$ | $0$ | $1/6$ | $1/12$ | $1/4$ |
| $\{1,2\}$ 与 $\{2,3\}$ | $0$ | $1/6$ | $1/12$ | $1/4$ |
| $\{1,3\}$ 与 $\{2,3\}$ | $1/12$ | $0$ | $1/12$ | $1/6$ |

三个上界都不超过目标 $\varepsilon=1/2$，因此这个例子**完整地满足 Proposition 1**。这里逐对计算得到的界比目标更好；一般证明为了同时处理任意 $m,s,I,J$，使用更统一的“三个 $\varepsilon/3$ 相加”的写法。

## 向老师汇报时，可以这样抓住主线

1. 旧日历 $X_1=V,X_2=V+1$ 已经做到“一次只看一个位置”的分布距离至多 $1/6$。
2. 旧日期作为指数，决定两个新间隔 $G_1,G_2$ 的均匀取值范围；大随机起点 $U$ 生成三个新日期。
3. 三种双日期观察记录改写为 $(U,G_1)$、$(U,G_1+G_2)$、$(U+G_1,G_2)$。
4. 宽均匀分布对小平移不敏感：第二种记录接近 $(U,G_2)$，第三种也接近 $(U,G_2)$。**这些都是给定旧日历后的分布估计。**
5. 理想记录只剩 $(U,G_1)$ 与 $(U,G_2)$；它们来自旧日期 $X_1,X_2$ 的同一随机处理，所以旧误差 $1/6$ 可以直接使用。最后用三角不等式把误差相加。

如果只记一个画面，就记住三条箭头：

$$
(U,G_1),\qquad
(U,G_1+G_2)\longrightarrow(U,G_2),\qquad
(U+G_1,G_2)\longrightarrow(U,G_2).
$$

第一条已经是理想形式；第二条由**宽间隔 $G_2$**掩盖 $G_1$；第三条由**宽起点 $U$**掩盖 $G_1$。这就是整个递归证明在最小非平凡例子中的核心。
